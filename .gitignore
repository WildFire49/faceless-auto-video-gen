# ── secrets ──
.env
*.env.local
client_secret*.json
token*.json
credentials*.json

# ── local state & generated media ──
rewind.db
rewind.db-wal
rewind.db-shm
projects/
trends_cache.json

# ── large / personal assets (keep folders, ignore contents) ──
assets/voice/*.wav
assets/music/*
!assets/music/bpm.yaml
assets/sfx/*
!assets/sfx/.gitkeep

# ── Go ──
/api/bin/
*.exe
*.test
*.out

# ── Python ──
.import_linter_cache/
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/
*.egg-info/

# NOTE: generated protobuf code (api/gen, ai/rewind, web/lib/gen) is
# deliberately COMMITTED, so a fresh clone builds without the proto toolchain
# and CI can fail on drift (SPEC.md 13.2).

# ── Node / Next.js ──
node_modules/
.next/
out/
*.tsbuildinfo
.npm

# ── editors / OS ──
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
next-env.d.ts
